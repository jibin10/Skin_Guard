export interface Charge {
  charge_description: string;
  charges_per: string;
  currency: string;
  convert_to: string;
  price: string;
  conversion: string;
  quantity: string;
  tax: string;
  amount: string;
  quote_id: string;
}
